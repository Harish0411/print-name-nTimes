// Print a name N times

var name=prompt("Enter your name");
var number=Number(prompt("Number of times"));

function no(){
     for(i=1; i<=number; i++){
	      document.write(name + "<br>");
		  }
	 }
	 no();